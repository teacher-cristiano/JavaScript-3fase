const v1 = 10
const v2 = "20"
const v3 = 15

const total = (v1 + v2) * v3

const total2 = v1 + v2

console.log(total)

console.log(total2)

const verificaPar = v3 % 2 === 0

console.log(`
    ${verificaPar ? "Número Par" : "Número Ímpar"}
    `)